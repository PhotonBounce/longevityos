data/feed.json is written by the daily literature sweep (.github/workflows/crawl.yml)
— never by hand. Until the first sweep lands, the app shows its bundled citation
library and labels it as exactly that.
